<Window
    x:Class="FuzzyLogic.UI.Views.AuthView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:FuzzyLogic.UI.Infrastructure.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dp="clr-namespace:FuzzyLogic.UI.Infrastructure.DP"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    xmlns:local="clr-namespace:FuzzyLogic.UI.Properties"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="800"
    Height="400"
    AllowsTransparency="True"
    Background="Transparent"
    MouseDown="Window_MouseDown"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle
                            Margin="2"
                            SnapsToDevicePixels="true"
                            Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                            StrokeDashArray="1 2"
                            StrokeThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="OptionMarkFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle
                            Margin="0,0,0,0"
                            SnapsToDevicePixels="true"
                            Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                            StrokeDashArray="1 2"
                            StrokeThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="RadioButton.Static.Background" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="RadioButton.Static.Border" Color="#FF707070" />
        <SolidColorBrush x:Key="RadioButton.Static.Glyph" Color="#FF212121" />
        <SolidColorBrush x:Key="RadioButton.MouseOver.Background" Color="#FFF3F9FF" />
        <SolidColorBrush x:Key="RadioButton.MouseOver.Border" Color="#FF5593FF" />
        <SolidColorBrush x:Key="RadioButton.MouseOver.Glyph" Color="#FF212121" />
        <SolidColorBrush x:Key="RadioButton.Pressed.Background" Color="#FFD9ECFF" />
        <SolidColorBrush x:Key="RadioButton.Pressed.Border" Color="#FF3C77DD" />
        <SolidColorBrush x:Key="RadioButton.Pressed.Glyph" Color="#FF212121" />
        <SolidColorBrush x:Key="RadioButton.Disabled.Background" Color="#FFE6E6E6" />
        <SolidColorBrush x:Key="RadioButton.Disabled.Border" Color="#FFBCBCBC" />
        <SolidColorBrush x:Key="RadioButton.Disabled.Glyph" Color="#FF707070" />
        <Style TargetType="{x:Type RadioButton}">
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="Width" Value="107" />
            <Setter Property="Height" Value="100" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <ControlTemplate.Resources>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="TextWrapping" Value="Wrap" />
                            </Style>
                        </ControlTemplate.Resources>

                        <Grid
                            x:Name="templateRoot"
                            Background="Transparent"
                            SnapsToDevicePixels="True">

                            <Border
                                x:Name="shadowBorder"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Visibility="Collapsed">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="0" Color="#FF00B9FF" />
                                </Border.Effect>
                            </Border>

                            <Border
                                x:Name="border"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="40" />
                                    </Grid.RowDefinitions>

                                    <Image
                                        Grid.Row="0"
                                        Width="47"
                                        Height="47"
                                        Source="{Binding Path=(dp:ButtonProperties.Image), RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RadioButton}}}" />
                                    <TextBlock
                                        Grid.Row="1"
                                        Margin="0,0,0,1"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontFamily="Century Gothic"
                                        FontSize="12"
                                        Text="{TemplateBinding ContentPresenter.Content}"
                                        TextAlignment="Center"
                                        TextWrapping="Wrap" />

                                </Grid>
                            </Border>

                            <Image
                                x:Name="doneImage"
                                Width="27"
                                Height="27"
                                Margin="-8"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Source="{StaticResource DoneImage}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="border" Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FF1240A4" />
                                            <GradientStop Offset="1" Color="#FFFF1818" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="doneImage" Property="Visibility" Value="Visible" />
                            </Trigger>

                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="border" Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Offset="0" Color="#FF979494" />
                                            <GradientStop Offset="0.6" Color="#CCCBC7C7" />
                                            <GradientStop Offset="1" Color="#FF979494" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="doneImage" Property="Visibility" Value="Hidden" />
                            </Trigger>

                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="False" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="shadowBorder" Property="Visibility" Value="Visible" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="White" CornerRadius="20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="325" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1">
                <TextBlock
                    Margin="0,10,0,0"
                    HorizontalAlignment="Center"
                    FontFamily="Candara Light"
                    FontSize="16"
                    Text="{x:Static local:Resources.SelectUserTypeTitle}" />

                <WrapPanel
                    Margin="0,10,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top">

                    <RadioButton
                        Grid.Row="0"
                        Grid.Column="0"
                        dp:ButtonProperties.Image="{StaticResource AdminPlateImage}"
                        Content="{x:Static local:Resources.AdminTitle}"
                        IsChecked="True" />

                    <RadioButton
                        Grid.Row="0"
                        Grid.Column="1"
                        dp:ButtonProperties.Image="{StaticResource ScientistImage}"
                        Content="{x:Static local:Resources.KnowledgeEngineerTitle}" />

                    <RadioButton
                        Grid.Row="1"
                        Grid.Column="0"
                        dp:ButtonProperties.Image="{StaticResource ResearcherImage}"
                        Content="{x:Static local:Resources.ResearcherTitle}"
                        IsChecked="True" />

                    <RadioButton
                        x:Name="RegistrationButton"
                        Grid.Row="1"
                        Grid.Column="1"
                        dp:ButtonProperties.Image="{StaticResource NewUserImage}"
                        Content="{x:Static local:Resources.AddUserTitle}" />

                </WrapPanel>

                <TextBlock
                    Margin="20,60,0,10"
                    HorizontalAlignment="Center"
                    FontFamily="Corbel Light"
                    FontSize="24">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{x:Static local:Resources.AuthTitle}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=IsChecked, ElementName=RegistrationButton}" Value="True">
                                    <Setter Property="Text" Value="{x:Static local:Resources.RegistrationTitle}" />
                                </DataTrigger>

                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>

                </TextBlock>

                <controls:CustomTextBox
                    Height="32"
                    VerticalContentAlignment="Center"
                    dp:TextBoxProperties.Icon="{StaticResource LoginImage}"
                    BorderThickness="0,1,0,1"
                    FontSize="18"
                    Foreground="#FF1F1F1F"
                    Text="kostyaLem" />
                <controls:CustomTextBox
                    Height="32"
                    VerticalContentAlignment="Center"
                    dp:TextBoxProperties.Icon="{StaticResource PasswordImage}"
                    BorderThickness="0,1,0,1"
                    FontSize="18"
                    Foreground="#FF1F1F1F"
                    Text="************" />

            </StackPanel>

            <Border
                Grid.Column="0"
                ClipToBounds="True"
                CornerRadius="20,0,0,20">
                <Border.Effect>
                    <BlurEffect Radius="5" />
                </Border.Effect>

                <Border.Background>
                    <ImageBrush ImageSource="{StaticResource AuthBackImage}" Stretch="UniformToFill" />
                </Border.Background>
            </Border>

            <TextBlock
                Margin="0,316,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                FontFamily="Leelawadee UI"
                FontSize="48"
                FontWeight="Bold"
                Foreground="White"
                RenderTransformOrigin="0.5,0.5"
                Text="Fuzzy Logic">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" />
                </TextBlock.Effect>
            </TextBlock>
        </Grid>
    </Border>
</Window>
